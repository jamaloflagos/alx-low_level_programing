#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: Computes absolute value of integers
 *
 * @n: Parameter
 *
 * Return: 0 or 1
 */

int _abs(int n)

{
	if (n >= 0)
	{
		return (n);
	}
	else

	{
		return (-n);
	}
}
